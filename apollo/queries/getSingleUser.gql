query($id: ID!, $limit: Int) {
  user(id: $id) {
    id
    username
    fullname
    image {
      id
      url
    }
    workouts(sort: "scheduled:desc", limit: $limit) {
      id
      scheduled
      ready
      sticky
      name
      createdAt
      feedback
      sections {
        id
        name
        complexes {
          name 
          id
          units {
            id
            sets
            reps
            time
            distance
            rest 
            remarks
            exercise {
              id
              name
              image {
                id
                url
              }
            }
          }
        }
      }
    }
  }
}