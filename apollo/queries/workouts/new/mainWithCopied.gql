query($id: ID!, $copiedWorkoutId: ID!) {
  user(id: $id) {
    id
    username
    workouts(limit: 3, sort: "scheduled:desc", where: { sticky_ne: "true", ready: "true" }) {
      id
      scheduled
      feedback
      sections {
        id
        name
        complexes {
          name 
          id
          units {
            id
            sets
            reps
            time
            distance
            rest
            remarks
            exercise {
              id
              name
              image {
                id
                url
              }
            }
          }
        }
      }
    }
  }
  copiedWorkout: workout(id: $copiedWorkoutId) {
    id
    scheduled
    sticky
    feedback
    user {
      id
      username
    }
    sections {
      id
      name
      complexes {
        name 
        id
        units {
          id
          sets
          reps 
          time
          distance
          rest
          remarks
          exercise {
            id
            name
            image {
              id
              url
            }
          }
        }
      }
    }
  }
}