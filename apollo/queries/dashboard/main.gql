query($id: ID!) {
  user(id: $id) {
    id
    username
    fullname
    email
    image {
      id
      url
    }
    workouts(limit: 1, sort: "scheduled:asc", where: { sticky_ne: "true" } ) {
      id
      scheduled
      ready
    }
  }
}