mutation($input: registerInput) {
  register(input: $input) {
    token
    user {
      id
      username
      fullname
      email
      admin
      active
      image {
        url
      }
    }
  }
}